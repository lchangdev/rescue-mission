<%= link_to "Create New Question", new_question_path %>
<ul>
  <% @questions.each do |question| %>
    <li><%= link_to "#{question.title}", question_path(question) %> - <%= link_to 'edit', edit_question_path(question) %> <%= link_to 'delete', question, method: :delete %><br>
</li>
  <% end %>
</ul>
